---
title: 'Can story points measure the growth AI gives you?'
subtitle: 'And if they can, how long does it take?'
date: '2026-08-19'
topic: 'Tech'
lang: en
interactive: true
chapter: 'Chapter IV'
description: 'A team asked to grow 10% per quarter, measured in story points. Working through the arithmetic — sample deviation, standard error, the two kinds of error — the target turns out to be smaller than the noise it must be read against, and the honest answer takes two and a half years.'
weather:
  time: '1–3 min'
  oneSitting: true
  load: 2
  needFirst: 'a little statistics will help'
  bestWhen: 'freetime'
rememberSentence: 'Your team swings 15% on its own. The improvement you are looking for is 10%.'
rememberAttribution: 'from Can story points measure AI?'
openDraft:
  startedAt: '2026-08-19'
  promise: 'no promises'
  note: 'English edition of the Vietnamese piece; section IV and the conclusion are freshly translated'
  sections:
    - label: 'I. Velocity and the basic arithmetic'
      state: 'editing'
    - label: 'II. Comparing two series'
      state: 'editing'
    - label: 'III. How big does a difference have to be?'
      state: 'editing'
    - label: 'IV. So how long does it take?'
      state: 'editing'
    - label: 'V. Conclusion'
      state: 'editing'
---

One of the companies I worked at leaned on AI heavily across the whole Software
Development Life Cycle (SDLC). Using that much AI is a form of investment, so the
CEO asked for productivity to improve by 10% every quarter — and productivity, on
the R&D (Research and Development) side, was measured in story points (SP).
Whether the story point is the right way to measure productivity is a different
argument and this piece will not have it; assume it is. The question here is narrower: is
"10% growth every quarter" something you can actually establish?

## Velocity and the basic arithmetic

In software the word "productivity" usually turns into the word "velocity", and
velocity is measured as the number of SP finished in one sprint. Productivity
over a quarter, then, is many sprints — which means it is not a single number at
all. It is a series.

Here is Q1, as logged:

```
38   45   31   42   36   48
```

What can we pull out of that?

The mean is $\mu_1=40$, and yet not one sprint was 40. The gap between the best
sprint and the worst is $48-31=17$ SP, and nothing on the outside changed across
those twelve weeks: same people, same codebase, same way of working.

So the first thing to accept is that **velocity swings even when nothing is
happening**. Not big changes — small ones. Somebody was off sick. A story turned
out easier than it looked, or harder.

That swing has a size, and the sample standard deviation is how you get at it:
$$\sigma_1 = \sqrt{\frac{1}{N-1} \sum_{i=1}^{N}(x_i - \mu_1)^2}$$ With $N=6$ and
the series above, $\sigma_1=6.23$, and the sample variance is $s_1^2=38.8$. A lot
of numbers, I know. The short version: **with nothing whatsoever going on, a
sprint can land 6.23 points off the mean.**

One more number falls out of the same data, the coefficient of variation $c$:
$$c_1=\frac{\sigma_1}{\mu_1}=\frac{6.23}{40}=0.156$$ which says that **this
team's ordinary swing is about 15.6% of its own average**. As teams go, that is
a steady one.

## Comparing two series

The question at the end is not any of the numbers above — trust me, they matter
— it is a comparison between two series.

Q2 comes in, and the log now reads:

```
Q1: 38  45  31  42  36  48   →  avg: 40.0
Q2: 41  49  36  47  40  51   →  avg: 44.0
```

Q2's average is 4 points above Q1's. Exactly 10%. Time to celebrate?

Celebrate first, sure. But then: **if nothing at all had acted on this team, AI
included, could a 4-point gap have shown up anyway?**

The usual way to compare two series is to compare their means, which is what we
just did — and a mean carries an error of its own, the standard error of the
mean: $$SE_1=\frac{\sigma_1}{\sqrt{N}}=\frac{6.23}{\sqrt{6}}=2.54$$ What that
number says is this: if you had the power to re-run Q1 in some other timeline,
same team, same conditions, the mean $\mu_1$ (40) coming out 2.54 points
different would be an ordinary Tuesday.

So if we could re-run *both* quarters in another timeline, how far apart could
the two means land?

<ReRun
	mu={40}
	sd={[6.23, 5.87]}
	n={6}
	at={4}
	atLabel="+4 — what we actually observed"
	axisLabel="difference between the two quarterly means (points)"
	readout={"You have re-run it {runs} times, in a world where nothing changed at all — no AI, no improvement, nothing. {hits} of those came out {at} points or more."}
	label="Figure 2"
	hint="re-run the quarter"
	caption="Each dot is a quarter lived again: two runs of six sprints drawn from the same team, the same spread, with no improvement anywhere in it. The orange dots are the times noise alone built 'evidence' as large as the evidence we actually have."
	alt="Dots fall around zero in a bell shape; those at +4 and beyond are inked in a second colour."
/>

With $SE_2=\frac{\sigma_2}{\sqrt{N}}=\frac{5.87}{\sqrt{6}}=2.39$,

$$SE=\sqrt{SE_1^2+SE_2^2}=\sqrt{2.54^2+2.39^2}=3.49$$

Which gives us something to say: **with nothing changed at all, two quarterly
means drifting 3.49 points apart is normal.** The gap we observed is 4 points,
and noise alone reaches 3.5. The quick conclusion is the honest one — there is
not enough here to say whether AI helped, because what we are holding is barely
distinguishable from noise.

## How big does a difference have to be?

Before the arithmetic, one more thing about that $SE$: it is also the width of
the bell the noise is spread along. Go back to re-running both quarters in
another timeline — do that endlessly and about 70% of the time the difference
lands between -3.49 and 3.49 points.

<NormalCurve
	sd={3.49}
	bandLabel="about 70% of runs land in here"
	bandTick="±SE = 3.49"
	axisLabel="difference between the two quarterly means (points)"
	marks={[{ at: 4, label: 'observed difference: +4' }]}
	label="Figure 1"
	hint="the noise distribution"
	caption="With nothing changed, the gap between two quarters still wanders around zero with a width of SE = 3.49. The +4 we observed sits well inside that cloud."
	alt="A normal curve centred on zero, shaded from -3.49 to 3.49 points, with a mark at +4 just past the edge of the shading."
/>

To detect anything and mean it, there are two ways to be wrong, and both have to
be guarded against.

### Type I error

A type I error is a false alarm: nothing changed, but noise happened to throw up
a gap wider than the ordinary one we just measured (3.49). To guard against it,
we allow only a 5% chance of noise clearing the bar — and on a normal curve that
point sits 1.96 widths out from the centre. So the bar is
$1.96\times3.49=6.84\approx7$, which means: **under 7 points, say nothing.**

<Sidenote>

  The 1.96 belongs to the normal distribution and holds when the sample is
  large. Here each quarter is six sprints, and $\sigma$ is itself estimated from
  those same six numbers, so strictly this calls for the _t_ distribution with
  Welch's degrees of freedom — 10 here — and a factor of 2.23 rather than 1.96.
  The bar moves from 7 to 8, and $\Delta_{min}$ below from 10 to 11. This piece
  keeps 1.96 to keep the thread clean, because the conclusion does not move: the
  10% being asked for (4 points) sits under both bars.

</Sidenote>

### Type II error

A type II error is a miss: the improvement was real, but noise dragged the number
below the bar and it got read as "nothing happened". Concretely — if the real
improvement is exactly the 7 points we just computed, then across endless re-runs
you catch it half the time and lose it the other half. To push the catch rate to
80%, the real improvement has to clear 7 by a margin: on a bell, shifting the
centre 0.84 widths further puts 80% of the distribution to the right of the bar.

So what is needed is $$\Delta_{min}=(1.96+0.84)\times SE=2.8\times 3.49=9.77\approx 10$$

Which is to say, **a gap of at least 10 points before it counts as improvement.**

<DetectionPower
	se={3.49}
	presets={[
		{ label: 'the bar', at: 6.84 },
		{ label: '80%', at: 9.77 }
	]}
	thresholdLabel="the bar: 6.84"
	axisLabel="difference between the two quarterly means (points)"
	readout={"If the real improvement is exactly {delta} points, then across endless re-runs you would call it an improvement about {power} of the time. The shaded tail is that share."}
	label="Figure 3"
	hint="drag it"
	caption="The grey bell standing still is noise. The blue one is a world where the improvement is real — drag it wherever you like. Park it on the bar and the shaded part is exactly half: that is where the 50% goes missing."
	alt="Two normal curves of equal width, one centred on zero and one moved by a slider, with a threshold rule and a shaded tail."
/>

## So how long does it take?

Let me restate the question. We know a gap has to reach at least 10 points before
it counts, and we collected $N=6$ per quarter. The new question is **"how many
sprints (N) does it take to establish that the team improved by 10%?"**

### Generalising SE

The $SE=3.49$ from earlier is one specific number for $n=6$, not a formula.
Assuming the two series are always the same length, it can be written out:

$$SE=\sqrt{SE_1^2 + SE_2^2} = \sqrt{\frac{\sigma_1^2}{n} + \frac{\sigma_2^2}{n}} = \sqrt{\frac{\sigma_1^2 + \sigma_2^2}{n}} = \sqrt{\frac{2}{n} \times \frac{\sigma_1^2 + \sigma_2^2}{2}} = \sigma_{pooled} \times \sqrt{\frac{2}{n}}$$

where

$$\sigma_{pooled}=\sqrt{\frac{\sigma_1^2 + \sigma_2^2}{2}} = \sqrt{\frac{38.8 + 34.4}{2}} = \sqrt{36.6} = 6.05$$

### Working the threshold backwards

We also have $\Delta_{min}=2.8 \times SE \approx 10$ as the point where a gap
counts. Substituting the formula for $SE$ gives an inequality:

$$\Delta \geq 2.8 \times SE = 2.8 \times \sigma\sqrt{\frac{2}{n}}$$

In words: **a real improvement has to be at least 2.8 times the noise of the
comparison.**

Now rearrange it for $n$.

$$\Delta\geq2.8\times\sigma\times\sqrt{\frac{2}{n}} \Leftrightarrow \Delta^2\geq2.8^2\times\sigma^2\times\frac{2}{n} \Leftrightarrow \Delta^2\geq\frac{15.7\times\sigma^2}{n} \Leftrightarrow n \geq \frac{15.7\times\sigma^2}{\Delta^2}$$

That is a formula for $n$, but not yet one in percentages. To get there, divide
top and bottom by $\mu^2$ — the square of the series' mean.

$$

n \geq \frac{15.7\times\left(\frac{\sigma^2}{\mu^2}\right)}{\left(\frac{\Delta^2}{\mu^2}\right)}

\Leftrightarrow n \geq \frac{15.7\times(\sigma/\mu)^2}{\Delta^2/\mu^2}

\Leftrightarrow n \geq \frac{15.7\times c^2}{(\Delta/\mu)^2}

$$

And $\sigma/\mu=c$ is the team's coefficient of variation. So what does the
formula say?

The numerator says _how much noise, in percent, this team brings_. The
denominator says _how much improvement, in percent, you want to catch_.

With the pooled $c$ across both quarters,
$c=\frac{\sigma_{pooled}}{\mu_{pooled}}=\frac{6.05}{42}=0.144$, and a 10%
improvement to catch, so $\Delta/\mu=0.1$, $n$ has to be

$$
n \geq \frac{15.7 \times 0.144^2}{0.1^2} \approx 32.5
$$

#### A quick check

Put $n=33$ back into the formula for SE:
$SE=\sigma\times\sqrt{\frac{2}{n}}=6.05\times\sqrt{\frac{2}{33}}\approx1.49$.
That gives $\Delta_{min}=2.8 \times SE=2.8\times1.49=4.17$, and
$4.17/\mu_{pooled}=4.17/42=0.099=9.9\%$. Good — the arithmetic holds.

So it takes at least **33 sprints per arm**, meaning **66 sprints**, before you
can catch whether something has lifted productivity by 10%.

## Conclusion

Back to the CEO's question: "productivity has to improve 10% every quarter". The
answer is not "yes" or "no". It is something more uncomfortable: **with one
quarter of data, that question has no answer at all.**

Three numbers from this piece, side by side, say the whole thing:

- The team already swings **15%** around its own average when nothing is
  happening.
- The gap to reach is **4 points**; the noise of the comparison is **3.49
  points**. What we need to measure is barely distinguishable from pure noise.
- To tell those two apart takes **33 sprints per arm** — with two-week sprints,
  two and a half years of data for **one** conclusion.

Here is the paradox. The review cycle is a quarter, six sprints, while the
measure being used to do the reviewing needs ten times that before it can
conclude anything. Asking for "10% a quarter" is not wrong as an expectation. It
is just **not verifiable in story points** — and an unverifiable demand gets
answered each quarter by instinct, by a number that happened to look good, or by
whoever was loudest in the meeting.

If you still want to measure it, the formula
$n \geq \frac{15.7 \times c^2}{(\Delta/\mu)^2}$ leaves exactly three knobs:

1. **Cut $c$ — make the team less noisy.** $n$ scales with $c^2$, so the steadier
   the team the less time it takes: pull the swing from 15% down to 7.5% and the
   wait drops to **a quarter** of what it was. Stories split small and evenly,
   estimates kept consistent, no changing the scale halfway through. This turns
   out to be the most valuable work on the list, and it has almost nothing to do
   with AI.
2. **Accept a bigger target.** $n$ scales inversely with $(\Delta/\mu)^2$, so
   hunting a 20% improvement costs a quarter of what hunting 10% costs. A genuinely
   large effect is easier to prove than a small one is to evidence.
3. **Widen the observation window.** Stop asking "did we grow 10% this quarter";
   ask "after a year and a half, has the trend actually turned".

And the last one, which matters most: none of the numbers above **conclude
anything about whether AI is useful**. They say story points are not sensitive
enough to serve as evidence for the question being asked. Those are two entirely
different statements — and in an end-of-quarter review they get treated as one.

<SampleSizeGrid
	first="38 45 31 42 36 48"
	second="41 49 36 47 40 51"
	unitLabel="sprints to observe, per arm"
	readoutEnough={"Your team swings {c} around its mean. The observed difference is {diff} points against a bar of {thr} points — enough to call it an improvement. To catch the level you picked, you need {n} sprints per arm, about {months} months."}
	readoutShort={"Your team swings {c} around its mean. The observed difference is {diff} points against a bar of {thr} points — not enough to say anything yet. To catch the level you picked, you need {n} sprints per arm, about {months} months."}
	label="Figure 4"
	hint="your team's numbers"
	caption="Two levers, both squared: halve the team's swing, or double the improvement you need to catch, and either one cuts the wait to a quarter. The filled cell is where the team in this piece stands."
	alt="A table of sprints to observe: rows are the improvement you want to catch, columns are how much the team varies."
/>
