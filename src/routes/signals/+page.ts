/**
 * The author's own page. Not part of the site: it is not prerendered, it is not
 * under a locale, and it never appears in the nav or the feeds — `/signals` is
 * typed by one person, and `hooks.server.ts` asks that person for a password
 * before this file is ever reached.
 */
export const prerender = false;
